from app.services import *


def setup_routers(inst):
    inst.router.add_get('/', index)
    # inst.router.add_get('/', index)
    # inst.router.add_get('/', index)
    # inst.router.add_get('/', index)
    # inst.router.add_get('/', index)
    # inst.router.add_get('/', index)
    # inst.router.add_get('/', index)
    # inst.router.add_get('/', index)
